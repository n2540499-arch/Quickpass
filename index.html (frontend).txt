<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>AeroRoute — Live</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <div class="container">
    <header>
      <h1>AeroRoute</h1>
      <p>Smart Traffic & Pollution Routing</p>
    </header>

    <main class="grid">
      <section class="card">
        <h2>Live Road Status</h2>
        <div>Road: <span id="roadId">road-1</span></div>
        <div>Congestion: <span id="congestion">-</span></div>
        <div>Pollution: <span id="pollution">-</span></div>
        <div>Last update: <span id="lastUpdate">-</span></div>
        <div>Weather: <span id="weather">-</span></div>
        <button id="refreshBtn">Refresh</button>
      </section>

      <section class="card">
        <h2>Request to Pass</h2>
        <label>Type
          <select id="vehicleType">
            <option value="normal">Normal</option>
            <option value="emergency">Emergency</option>
          </select>
        </label>
        <label>Vehicle Code
          <input id="vehicleCode" placeholder="Enter code (e.g. NRM-111)" />
        </label>
        <button id="requestBtn">Request Pass</button>
        <p id="requestResult"></p>
      </section>

      <section class="card">
        <h2>Admin — Manage Codes</h2>
        <label>New code
          <input id="newCode" placeholder="Code (e.g. EMG-101)" />
        </label>
        <label>Type
          <select id="newType"><option value="emergency">Emergency</option><option value="normal">Normal</option></select>
        </label>
        <button id="addCodeBtn">Add Code</button>
        <p id="adminStatus"></p>

        <h3>Current allowed codes</h3>
        <pre id="codesList" class="codes"></pre>
      </section>
    </main>

    <footer>
      <small>Demo only — do not connect to real traffic lights without permission.</small>
    </footer>
  </div>

  <script src="script.js"></script>
</body>
</html>
